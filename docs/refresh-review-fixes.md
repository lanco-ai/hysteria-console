# 刷新与冗余代码审查修复

- 修正流量分析四张指标卡的 `.metric-v` 绑定，保留用户详情 `.v` 分支。
- 手动刷新同时更新已展开的历史明细；收起时只失效缓存，重新展开才请求。显示更新时间，加载中重复刷新合并为一次后续请求。
- GET/HEAD/POST 使用惰性 ContextVar 倍率快照；同一请求只读取一次，下一请求重新读取，异常后重置；不修改倍率值或计费算法。
- 将设备上限解析统一到 `user_compat.configured_max_devices`，保留旧模块入口兼容调用。
- 合并近期工作台 CSS 的蓝色与中性色重复覆盖，保持当前布局与色彩；未对整个历史样式表做大范围重写。
- 修正旧敏感字段测试：检查实际密码哈希及编辑弹窗内容，不把开发注释误判成泄露。

验证：123 项 Python 测试通过（2 条既有 datetime.utcnow 弃用警告）；工作台展开布局和动态刷新浏览器测试通过；线上资源与源码及部署后端函数 AST 一致。

备份：`/root/hysteria/backups/refresh-review.HH81q0/`。仅重启面板服务，未修改用户、用量、订阅模板及代理运行配置。
